# SwiftPM
/.build/
/.swiftpm/
Package.resolved

# Local build output (the .app / .dmg are release artifacts, not sources)
/build/
/dist/

# macOS
.DS_Store

# Local signing material — never commit keys
*.p12
*.cer
*.key
/.secrets/

# Screenshot workbench: throwaway demo database, raw captures and the capture guide.
# Regenerate with .demo/CAPTURE.md; never a build input.
.demo/

# Stray local Python virtualenv, not part of the project.
/.venv/
